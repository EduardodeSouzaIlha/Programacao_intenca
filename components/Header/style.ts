import styled from 'styled-components';

export const HeaderWrapper = styled.div`
    border-bottom: white 2px solid;
    background: linear-gradient(45deg,rgb(40, 6, 80),rgb(28, 32, 32),rgb(25, 32, 32));
    position: fixed;
    width: 100vw;
    height: 10vh;
    display: flex;
    align-items: center;
    justify-content: center;
`
export const ImageWrapper = styled.div`
    width: 12.5vw;
    /* display: flex;
    place-items: center; */
    position: relative;
    text-align: center;

`
